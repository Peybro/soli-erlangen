import * as React from "react";
import Layout from "../../components/layout";

export default function Geschichte() {
  return (
    <Layout pageTitle="Geschichte">
      <h3 className="sub-h">Geschichte</h3>
    </Layout>
  );
}
